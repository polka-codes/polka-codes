name: Issue Handler

on:
  issues:
    types: [opened, labeled]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue number to process'
        required: true
        type: number

jobs:
  handle:
    if: |
      (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'polka.codes')) ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Process issue and format task
        id: process
        uses: actions/github-script@v7
        with:
          script: |
            const handler = require('./.github/workflows/scripts/issue-handler.cjs');

            try {
              const issueData = await handler.getIssueData(github, context, context.payload.inputs);
              const taskString = handler.formatTask(issueData);

              core.setOutput('task', taskString);
              core.setOutput('issue_number', issueData.number);
            } catch (error) {
              core.setFailed(`Failed to process issue: ${error.message}`);
              throw error;
            }

      - name: Config git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b polka-codes/issue-${{ steps.process.outputs.issue_number }}

      - name: Run polka.codes CLI and commit changes
        env:
          POLKA_API_PROVIDER: openrouter
          POLKA_MODEL: deepseek/deepseek-chat
          POLKA_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "${{ steps.process.outputs.task }}"
          echo "${{ steps.process.outputs.task }}" | bun cli -v
          bun cli commit -a
          git push origin polka-codes/issue-${{ steps.process.outputs.issue_number }}
          bun cli pr

# Generated by polka.codes
