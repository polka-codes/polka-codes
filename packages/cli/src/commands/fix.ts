// generated by polka.codes
import { Command } from 'commander'
import { runWorkflow } from '../runWorkflow'
import { type FixWorkflowInput, fixWorkflow } from '../workflows/fix.workflow'

export const fixCommand = new Command('fix')
  .description('Fix issues by running a command and letting an agent fix it.')
  .argument('[command]', 'The command to run')
  .action(async (command: string | undefined, _options: any, cmd: Command) => {
    const workflowInput: FixWorkflowInput = { command }
    await runWorkflow('fix', fixWorkflow, cmd, workflowInput)
  })
