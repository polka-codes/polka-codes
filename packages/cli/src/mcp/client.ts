// generated by polka.codes

import type { IMcpClient } from './types'

/**
 * Factory function to create an MCP client using the official SDK
 */
export async function createMcpClient(
  serverName: string,
  config: { command: string; args?: string[]; env?: Record<string, string> },
): Promise<IMcpClient> {
  const { SdkMcpClient } = await import('./sdk-client.js')
  const client = new SdkMcpClient(serverName, config)
  await client.connect()
  return client
}
