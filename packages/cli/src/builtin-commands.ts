// generated by polka.codes

/**
 * List of built-in command names that cannot be used for custom scripts or skills.
 * This list is centralized to prevent drift across the codebase.
 */
export const BUILT_IN_COMMANDS = [
  'code',
  'commit',
  'pr',
  'review',
  'fix',
  'plan',
  'task',
  'workflow',
  'run',
  'init',
  'meta',
  'skills',
] as const

export type BuiltInCommand = (typeof BUILT_IN_COMMANDS)[number]
